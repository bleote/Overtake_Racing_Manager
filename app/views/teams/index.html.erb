<div class="row justify-content-center col-lg-6 col-md-8">
  <div class="py-4 text-center">
    <h1>Teams</h1>
    <div class="teams-list pt-2">
      <% @teams.each do |team| %>
        <p class="pb-3"><%= link_to "#{team.team_name}", team, class: "primary-btn" %></p>
      <% end %>
    </div>

    <div class="pt-3">
      <%= link_to "Back", root_path, class: "btn btn-warning" %>
    </div>
  </div>
</div>
